# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas (or local) connection string
MONGO_URI=mongodb+srv://debugwithmahesh_db_user:IF7yyf38eBISD040@cms.nv98clt.mongodb.net/?appName=cms
# JWT
JWT_SECRET=replace_this_with_a_long_random_secret
JWT_EXPIRE=7d

# CORS - your deployed frontend URL
CLIENT_URL=http://localhost:5173

# Email notifications (optional). If SMTP_HOST is left blank, emails are
# skipped and just logged to the console — the app runs fine without this.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=College Management System <no-reply@cms.edu>
